
#STL Introduction

##六大组件

1. 容器

    各种数据结构, 用来存放数据
2. 算法

    各种例如sort, search的常用算法
3. 迭代器

    容器和算法的胶合器, 是所谓的泛型指针
4. 仿函数

    行为类似函数, 可作为算法的某种策略. 实现上来说, 是一种重载了operator()的class或class template
5. 配接器

    修饰容器或仿函数的或迭代器的东西.
6. 配置器

    负责空间配置和管理
